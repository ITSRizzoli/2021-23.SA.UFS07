import React from 'react';
import PropTypes from 'prop-types';

export const Text = ({}) => {
    return (
        <span>Ciao Mondo!</span>
    )
}

Text.propTypes = {

};

Text.defaultProps = {
    // label: 'Button',
    // type: 'reset',
};
