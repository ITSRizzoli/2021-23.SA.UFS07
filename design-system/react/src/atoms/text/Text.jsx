import React from 'react';
import PropTypes from 'prop-types';

export const Text = ({}) => {
    return(
        <span>Ciao mondo</span>
    );
};

Text.prototype={
    
};

Text.defaultProps = {
    
}